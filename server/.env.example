# server/.env の見本(実際の値は server/.env に書き、.env はGitHubに上げない)
GEMINI_API_KEY=
# 学習メモ(YYYYMMDD_学習メモ.txt)が入っているフォルダ
RAG_DATA_DIR=C:\work\学習\202609
# 開発用DB(docker compose で起動したPostgreSQL)への接続先
# パスワード部分は、ai-chat-app直下の .env の POSTGRES_PASSWORD と同じ値にする
DATABASE_URL=postgres://rag:ここにパスワード@127.0.0.1:5432/ragdb
